{
  "name": "proxy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'Nothing to build'",
    "dev": "docker-compose up --build",
    "dev:stop": "docker-compose down",
    "deploy": "docker build -t proxy-prod . && docker push proxy-prod"
  }
}
